/**
 * API SERVICE - AXIOS CONFIGURATION
 * 
 * Setup:
 * - Create axios instance with baseURL from env
 * - Add request interceptors for auth (if needed)
 * - Add response interceptors for error handling
 * 
 * Functions to export:
 * - startStory(genre)
 * - continueStory(sessionId, choice)
 * - finalizeStory(sessionId)
 * - getStory(sessionId)
 * 
 * Imports needed:
 * // import axios from 'axios';
 */