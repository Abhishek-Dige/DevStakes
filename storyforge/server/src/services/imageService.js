/**
 * IMAGE GENERATION SERVICE
 * 
 * Functions to implement:
 * - generateSceneImage(prompt, genre) - Generate image URL using Pollinations.ai
 * - enhancePrompt(basePrompt, genre) - Add genre-specific styling to prompt
 * - getGenreStyle(genre) - Return style keywords for each genre
 * 
 * Uses Pollinations.ai API (no auth required):
 * https://image.pollinations.ai/prompt/{encodedPrompt}?width=1024&height=768&nologo=true&model=flux
 */